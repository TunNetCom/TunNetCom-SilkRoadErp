<Project>
  <PropertyGroup>
    <!-- Override nullable for SQL Database project - C# 7.3 doesn't support nullable -->
    <Nullable>disable</Nullable>
    <LangVersion>7.3</LangVersion>
  </PropertyGroup>
</Project>

