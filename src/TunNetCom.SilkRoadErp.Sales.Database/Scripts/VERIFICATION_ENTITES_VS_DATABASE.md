# Vérification : Cohérence entre Entités EF Core et Définitions SQL

## ✅ Tables avec DECIMAL(18, 3) - CORRECT

### Table Produit
- **SQL**: `prix DECIMAL(18, 3)`, `prixAchat DECIMAL(18, 3)`
- **EF Core**: `decimal(18, 3)` ✅
- **Fichier**: `ProduitConfiguration.cs`

### Tables Ligne* (toutes les lignes de commandes/livraisons)
- **LigneBL**: `prix_HT`, `tot_HT`, `tot_TTC` → DECIMAL(18, 3) ✅
- **LigneCommandes**: `prix_HT`, `tot_HT`, `tot_TTC` → DECIMAL(18, 3) ✅
- **LigneDevis**: `prix_HT`, `tot_HT`, `tot_TTC` → DECIMAL(18, 3) ✅
- **LigneBonReception**: `prix_HT`, `tot_HT`, `tot_TTC` → DECIMAL(18, 3) ✅
- **LigneAvoirs**: `prix_HT`, `tot_HT`, `tot_TTC` → DECIMAL(18, 3) ✅
- **LigneAvoirFournisseur**: `prix_HT`, `tot_HT`, `tot_TTC` → DECIMAL(18, 3) ✅

**Toutes les configurations EF Core correspondent** ✅

## ✅ Tables avec DECIMAL(18, 2) - CORRECT (non affectées)

Ces tables ne sont pas liées aux produits et gardent DECIMAL(18, 2) :
- **Transaction**: `montant` → DECIMAL(18, 2) ✅
- **Systeme**: `Timbre`, `pourcentageFodec` → DECIMAL(18, 2) ✅
- **EcheanceDesFournisseurs**: `montant` → DECIMAL(18, 2) ✅
- **Devis**: `tot_H_tva`, `tot_tva`, `tot_ttc` → DECIMAL(18, 2) ✅
- **BonDeLivraison**: `tot_H_tva`, `tot_tva`, `net_payer` → DECIMAL(18, 2) ✅
- **AvoirFinancierFournisseurs**: `tot_ttc` → DECIMAL(18, 2) ✅

## 📋 Résumé

**Toutes les entités Entity Framework sont cohérentes avec les définitions SQL du projet de base de données.**

Les tables Produit et toutes les tables Ligne* utilisent DECIMAL(18, 3) dans :
- ✅ Les fichiers SQL de définition (`dbo/Tables/*.sql`)
- ✅ Les configurations Entity Framework (`Entites/Configurations/*Configuration.cs`)

**Prêt pour application sur une nouvelle base de données restaurée.**

